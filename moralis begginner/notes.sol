 // constant means that the value will always be the same
    uint public  constant myUint = 1; // number > 1
    int public myInt = -1; // number < 1
    string public myString = "A string"; // ususally text
    bool public myBool = true; // true or false values
